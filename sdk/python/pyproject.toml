[project]
name = "inferops"
version = "0.0.0"
description = "Python SDK for deploying inference runtimes to Kubernetes."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
