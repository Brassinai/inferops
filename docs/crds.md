# CRDs

The Kubernetes API surface will be built around custom resources such as:

- `ModelDeployment`
- `ModelRuntime`
- `ModelCache`

CRD schemas, validation, defaults, and compatibility rules will be defined before implementation hardens.
