# Routing

The gateway will route OpenAI-compatible requests to nano-vLLM runtime pods.

Routing design should cover service discovery, rollout-aware traffic behavior, authentication hooks, and operational metrics.
