profile: homelab

gpu:
  required: false

cache:
  root: /var/lib/inferops/models
  nodeSelector: {}
  capacityAnnotation: inferops.dev/cache-capacity
  requiredNodeResources: []

resources:
  requests:
    cpu: 50m
    memory: 96Mi
  limits:
    cpu: 500m
    memory: 512Mi
