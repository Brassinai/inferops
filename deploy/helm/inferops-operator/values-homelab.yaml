profile: homelab

gpu:
  required: true

cache:
  root: /var/lib/inferops/models
  nodeSelector: {}
  capacityAnnotation: inferops.dev/cache-capacity
  requiredNodeResources:
    - nvidia.com/gpu

resources:
  requests:
    cpu: 50m
    memory: 96Mi
  limits:
    cpu: 500m
    memory: 512Mi
